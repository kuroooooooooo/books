<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="communityReplyMapper">

	<select id="readReply" resultType="com.books.TO.ReplyDTO">
		SELECT
			rno, content, reply_id, regdate
		FROM
			book_reply
		WHERE bno = #{bno}
	</select>

</mapper>